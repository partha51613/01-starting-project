export interface User {
    id: string;
    avatar: string;
    name: string;
  }; //Using Interface  we can define object types only


//   This can also be writte in the format as shown below:

// type User = {
//     id: string;
//     avatar: string;
//     name: string;
//   }; //Using 'type' we can define object and other different types
